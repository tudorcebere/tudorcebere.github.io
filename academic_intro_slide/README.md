# Academic Intro Slide

This folder contains a single-slide LaTeX Beamer introduction for Tudor Cebere.

Build it with:

```sh
pdflatex tudor_cebere_academic_intro.tex
```

The slide uses the profile image at `../assets/img/tudor.jpg`, so compile from this folder or keep the same relative layout.
